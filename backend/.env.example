# Gemini API Key (Required for AI features)
GEMINI_API_KEY=your_gemini_api_key_here

# Database Configuration (Optional - local fallback used if unconfigured)
DB_HOST=localhost
DB_NAME=campus_board
DB_USER=root
DB_PASSWORD=
